
import { pgTable } from 'drizzle-orm/pg-core';

// No specific tables are required for this task as it's a simple 'hello' message.
// However, to satisfy Drizzle's schema export requirement, we export an empty object.
export const tables = {};
